name = "hotmart-sync"
main = "index.js"
compatibility_date = "2024-01-01"

# Cron desligado em 2026-07-09: substituído pela Edge Function
# hotmart-backfill (supabase/functions/hotmart-backfill), que roda a mesma
# lógica mas funciona de verdade — este worker recebia 400 da Hotmart
# (suspeita de WAF/anti-bot reagindo especificamente ao Cloudflare Workers;
# a mesma chamada funciona sem erro no Supabase Edge Functions). Arquivo
# fica no repositório como histórico, não é mais chamado.
# [triggers]
# crons = ["0 12 * * *"]
